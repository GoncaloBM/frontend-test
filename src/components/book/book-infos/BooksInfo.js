import { React } from "react";

// This component is only to organize the info of the Book component
export const BooksInfo = ({ children }) => {
  return <div className="organize">{children}</div>;
};
